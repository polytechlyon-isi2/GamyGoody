{% extends 'layout.html.twig' %}
{% set adminMenu = true %}

{% block title %}{{ title }}{% endblock %}

{% block content %}
<h2 class="text-center">{{ block('title') }}</h2>
{% for flashMessage in app.session.flashbag.get('success') %}
<div class="alert alert-success">
    {{ flashMessage }}
</div>
{% endfor %}

<div class="well">
{% form_theme articleForm '_field-prototype.html.twig' %}
{{ form_start(articleForm, { 'attr': {'class': 'form-horizontal'} }) }}
    <div class="form-group">
        {{ form_label(articleForm.title, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(articleForm.title) }}
            {{ form_widget(articleForm.title, { 'attr':  {
                'class': 'form-control'
            }}) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(articleForm.content, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(articleForm.content) }}
            {{ form_widget(articleForm.content, { 'attr':  {
                'class': 'form-control',
                'rows': '8'
            }}) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(articleForm.game, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(articleForm.game) }}
            {{ form_widget(articleForm.game, { 'attr':  {
                'class': 'form-control',
            }}) }}
        </div>
    </div>
    <div class="form-group">
        {{ form_label(articleForm.category, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(articleForm.category) }}
            {{ form_widget(articleForm.category, { 'attr':  {
                'class': 'form-control',
            }}) }}
        </div>
    </div>
    
    <div class="form-group">
        {{ form_label(articleForm.image, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(articleForm.image) }}
            {{ form_widget(articleForm.image.file, { 'attr':  {
                'class': 'form-control',
            }}) }}
        </div>
    </div>

    {% macro collectionImage(formData) %}
        {{ form_errors(formData) }}
        <div class="row">
            <div class="col-lg-9 col-md-8">
                {{ form_widget(formData.image.file, { 'attr':  {
                'class': 'form-control filestyle',
                }}) }}
            </div>
            <div class="col-lg-2 col-md-3 col-xs-9">
                {{ form_widget(formData.level, { 'attr':  {
                'class': 'form-control',
                }}) }}
            </div>
            <div class="text-center btn-spr col-xs-1 col-xs-offset-1 col-md-offset-0 "></div>
        </div>
    {% endmacro %}


    {#<div class="form-group">

        {{ form_label(articleForm.images, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label'
        }}) }}
        <div class="col-sm-6">
            {{ form_errors(articleForm.images) }}
            {{ form_widget(articleForm.images, { 'attr':  {
                'class': 'form-control',
                'data-prototype': '{{ _self.collectionImage(articleForm.images.vars.prototype)|e }}',
            }}) }}
        </div>
        {{ form_label(articleForm.images, null, { 'label_attr':  {
            'class': 'col-sm-4 control-label',
        }}) }}
        <div class="col-sm-6" id="article_images">
            <ul id="article_images" class="list-group" data-prototype='{{ _self.collectionImage(articleForm.images.vars.prototype)|e }}'>
                {% for image in articleForm.images %}
                    <li class="list-group-item">
                    {{ _self.collectionImage(image) }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>#}

    <div class="form-group">
        <div class="col-sm-offset-4 col-sm-3">
            <input type="submit" class="btn btn-primary" value="Save" />
        </div>
    </div>
{{ form_end(articleForm) }}
</div>
{% endblock %}


{#{% block script %}
<script type="text/javascript">
    var $collectionHolder;

    // setup an "add a tag" link
    var $addImageLink = $('<a href="#" class="btn btn-block btn-sm btn-primary add_image_link"><span class="glyphicon glyphicon-plus"></span> Ajouter une image</a>');
    var $newLinkLi = $('<li class="list-group-item"></li>').append($addImageLink);

    jQuery(document).ready(function() {
        // Get the ul that holds the collection of tags
        $collectionHolder = $('#article_images');

        // add a delete link to all of the existing tag form li elements
        $collectionHolder.find('li').each(function() {
            addImageFormDeleteLink($(this));
        });

        // add the "add a tag" anchor and li to the tags ul
        $collectionHolder.append($addImageLink);

        // count the current form inputs we have (e.g. 2), use that as the new
        // index when inserting a new item (e.g. 2)
        $collectionHolder.data('index', $collectionHolder.find(':input').length);

        $addImageLink.on('click', function(e) {
            // prevent the link from creating a "#" on the URL
            e.preventDefault();

            // add a new tag form (see next code block)
            addImageForm($collectionHolder, $newLinkLi);
        });

        function addImageForm($collectionHolder, $newLinkLi) {
            // Get the data-prototype explained earlier
            var prototype = $collectionHolder.data('prototype');

            // get the new index
            var index = $collectionHolder.data('index');

            // Replace '__name__' in the prototype's HTML to
            // instead be a number based on how many items we have
            var newForm = prototype.replace(/__name__/g, index);

            // increase the index with one for the next item
            $collectionHolder.data('index', index + 1);

            // Display the form in the page in an li, before the "Add a tag" link li
            var $newFormLi = $('<li class="list-group-item"></li>').append(newForm);
            $newLinkLi.before($newFormLi);

            addImageFormDeleteLink($newFormLi);
        }

        function addImageFormDeleteLink($imageFormLi) {

            var $removeFormA = $('<a href="#" class="btn btn-danger pull-right"><span class="glyphicon glyphicon-remove"></span></a>');
            $imageFormLi.find('div.btn-spr').append($removeFormA);

            $removeFormA.on('click', function(e) {
                // prevent the link from creating a "#" on the URL
                e.preventDefault();
                // remove the li for the tag form
                $imageFormLi.remove();
            });
        }

    });
 </script>
{% endblock %}#}